# =============================================================================
# AIOStreams Production Environment Configuration
# =============================================================================
# Copy this file to .env.prod and fill in your values:
#   cp .env.example .env.prod
#
# Then start with:
#   docker-compose -f docker-compose.prod.yml --env-file .env.prod up -d
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION
# -----------------------------------------------------------------------------
DB_USER=aiostreams_user
DB_PASSWORD=CHANGE_ME_USE_STRONG_PASSWORD
DB_NAME=aiostreams

# -----------------------------------------------------------------------------
# AIOSTREAMS CORE SETTINGS
# -----------------------------------------------------------------------------
# Name displayed in Stremio
ADDON_NAME=AIOStreams

# Unique identifier for your addon
ADDON_ID=com.aiostreams.addon

# Secret key for encrypting addon configurations (REQUIRED)
# Generate with: openssl rand -hex 32
# Must be exactly 64 hexadecimal characters
SECRET_KEY=GENERATE_ME_WITH_openssl_rand_hex_32

# Base URL where your addon is accessible (set to your domain)
# Example: https://aiostreams.yourdomain.com
BASE_URL=

# Log level: error, warn, info, http, verbose, debug, silly
LOG_LEVEL=info

# Disable rate limits for self-hosted instance
DISABLE_RATE_LIMITS=true

# -----------------------------------------------------------------------------
# ADDON PASSWORD (OPTIONAL)
# -----------------------------------------------------------------------------
# Extra layer of protection - users must enter this to configure the addon
# With Cloudflare Access enabled, this is optional but adds defense-in-depth
# Supports multiple passwords separated by commas: pass1,pass2,pass3
# ADDON_PASSWORD=your-secret-password

# -----------------------------------------------------------------------------
# CLOUDFLARE TUNNEL
# -----------------------------------------------------------------------------
# Get this from Cloudflare Zero Trust dashboard:
# 1. Go to https://one.dash.cloudflare.com/
# 2. Navigate to Zero Trust > Networks > Tunnels
# 3. Create a new tunnel and copy the token
TUNNEL_TOKEN=YOUR_CLOUDFLARE_TUNNEL_TOKEN

# -----------------------------------------------------------------------------
# DEBRID SERVICES (OPTIONAL)
# -----------------------------------------------------------------------------
# Add your debrid service API keys here
# These set default values for users who don't provide their own

# Real-Debrid
# DEFAULT_REALDEBRID_API_KEY=

# AllDebrid
# DEFAULT_ALLDEBRID_API_KEY=

# TorBox
# DEFAULT_TORBOX_API_KEY=

# Premiumize
# DEFAULT_PREMIUMIZE_API_KEY=

# Debrid-Link
# DEFAULT_DEBRIDLINK_API_KEY=

# -----------------------------------------------------------------------------
# TMDB (OPTIONAL - for better title matching)
# -----------------------------------------------------------------------------
# Get your API key from https://www.themoviedb.org/settings/api
# TMDB_API_KEY=
# TMDB_ACCESS_TOKEN=

# -----------------------------------------------------------------------------
# BUILT-IN ADDON CONFIGURATION (OPTIONAL)
# -----------------------------------------------------------------------------
# StremThru instance URL for debrid integration
# BUILTIN_STREMTHRU_URL=

# Bitmagnet instance URL (self-hosted BitTorrent indexer)
# BUILTIN_BITMAGNET_URL=

# -----------------------------------------------------------------------------
# PROXY CONFIGURATION (OPTIONAL)
# -----------------------------------------------------------------------------
# MediaFlow Proxy or StremThru for stream proxying
# DEFAULT_PROXY_ENABLED=false
# DEFAULT_PROXY_ID=mediaflow
# DEFAULT_PROXY_URL=
# DEFAULT_PROXY_CREDENTIALS=

# -----------------------------------------------------------------------------
# CACHE SETTINGS (OPTIONAL)
# -----------------------------------------------------------------------------
# Stream cache TTL in seconds (-1 to disable)
# STREAM_CACHE_TTL=3600

# Manifest cache TTL in seconds
# MANIFEST_CACHE_TTL=86400

# -----------------------------------------------------------------------------
# ADVANCED SETTINGS (OPTIONAL)
# -----------------------------------------------------------------------------
# Default request timeout in milliseconds
# DEFAULT_TIMEOUT=7000

# Maximum addons per configuration
# MAX_ADDONS=20
